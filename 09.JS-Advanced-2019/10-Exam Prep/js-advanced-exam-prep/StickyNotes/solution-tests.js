let calculatorClass = require('./solution');

let assert = require('chai').assert;

describe('should contain property expenses', function () {
    // let cal = new calculatorClass();
    //
    // let result = lookupChar(testData, 1);
    //
    // assert.equal(result, undefined);
});